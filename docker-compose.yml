services:
  ulamgym-nano:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    command: ["ulamgym-nano", "serve", "--task-dir", "data/sample_tasks", "--host", "0.0.0.0", "--port", "8000"]
